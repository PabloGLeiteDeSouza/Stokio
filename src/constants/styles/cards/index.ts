import { config } from '@gluestack-ui/config';

export const CardCustonColorsBg = {
  primary_light: config.tokens.colors.coolGray200,
  primary_dark: config.tokens.colors.coolGray900,
  secondary_light: config.tokens.colors.blueGray200,
  secondary_dark: config.tokens.colors.blueGray900,
  success_light: config.tokens.colors.green200,
  success_dark: config.tokens.colors.green900,
  warn_light: config.tokens.colors.yellow200,
  warn_dark: config.tokens.colors.yellow900,
  danger_light: config.tokens.colors.red200,
  danger_dark: config.tokens.colors.red900,
};
